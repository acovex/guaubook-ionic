import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'guabook-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
